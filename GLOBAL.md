# CRM — B2B Calls — Global Project Tracker

This file lives at the **project root** (outside `.claude/`) and tracks progress for **both developers**. Unlike `.claude/CLAUDE.md` (which is Dev A's private session memory for Claude Code), this file is the shared source of truth both of you update and both of you read before starting a new day.

**Rule: update the relevant row in the table below at the end of every work session, on both machines.** Keep entries short — one line of what shipped, one line of what's blocking, nothing more. Full detail belongs in each dev's own `.claude/CLAUDE.md`, not here.

---

## Project

- **Name:** CRM — B2B Calls (web-based CRM for a B2B call center)
- **Repo:** `git@github.com:SaadAbdullaH3/crm-b2b-calls.git` (public). Day 1 foundation is pushed — **Dev B clones this rather than rebuilding Day 1.** First-time setup: `cp .env.example .env && docker compose up -d && npm ci && npx prisma migrate dev && npm run db:seed && npm run dev`. Use `npm ci`, not `npm install`, so the lockfile pins hold (notably Prisma 6.19.3).
- **Spec:** `docs/crm-b2b-build-plan.md` (feature correlation + 10-day plan) — original SRS was `CRM_B2B_Calls_Final_Requirements.docx`, v1.0
- **Stack:** Next.js (App Router, custom server) + TypeScript + PostgreSQL + Prisma + Socket.io + Tailwind/shadcn
- **Deployment target:** self-hosted VPS (not Vercel) — Nginx + PM2/Docker
- **Timeline:** 10 working days
- **Team:** Dev A = Saad — "Core Calling Pipeline". Dev B = teammate — "Ops & People Console".
- **VC Dialer API status:** ⚠️ unconfirmed as of plan creation — assumed NOT available; clipboard fallback is being built first. Update this line the moment it's confirmed either way, since it affects Day 5 and Day 9 for Dev A.

---

## Day-by-Day Status

| Day | Dev A (Saad) — Core Calling Pipeline | Status | Dev B (Teammate) — Ops & People Console | Status | Blockers / Notes |
|---|---|---|---|---|---|
| 1 | Shared: repo scaffold, Prisma schema, Postgres setup, Auth/RBAC middleware, base layouts, Socket.io skeleton, seed 8 users | **Done (Dev A)** — 25 tables migrated, auth + RBAC live, 4 role shells, 8 users seeded, Socket.io + cron running | Shared foundation reused, not rebuilt — cloned and running locally | **Done (Dev B)** — 25 tables, seed OK (33 perms / 4 roles / 6 dispositions / 8 users), login + `/api/auth/me` verified, NF-07 index confirmed present, typecheck clean | Day 1 shipped by Dev A and pushed 2026-09-09; Dev B skips rebuilding it so both tracks can start Day 2 in parallel. **Use `npm ci`** — a fresh `npm install prisma` resolves to 7.x, which is a different product. |
| 2 | Lead Import Engine: upload, column mapping, parsing, duplicate detection, phone-format validation | **Done (Dev A)** — LM-01/02/04/05 shipped: `lead_import_rows` table (32 total), 3 API routes, 2 screens, exceljs parsing, U.S. phone format check, duplicate detection vs existing leads and within the file. Verified per-row against an 11-row fixture. Build + typecheck clean | Admin Configuration: user CRUD, roles/permissions, groups, dynamic field builder, dialer settings | **Done (Dev B)** — AD-01/02/03/05/06/07 shipped: 4 new tables (29 total), 11 API routes, 6 screens, 2 new permission keys. TM-05 + 4 lockout guards enforced at the API and verified. Build + typecheck clean | **Dev A: two things for you.** (1) `GET /api/admin/fields` returns the active dynamic fields your Day 2 column-mapper should offer as targets — values belong in `leads.custom_fields` keyed by `key`. (2) Re-running `npm run db:seed` now wipes runtime permission changes — see Cross-Cutting Decisions. |
| 3 | Import: validation summary, error export, import history, lead source tagging. Start Lead Assignment: request UI | Not started | Communication Module: DMs, groups, broadcasts, announcements, notification schema + Socket.io wiring | **Done (Dev B)** — conversation model, DMs, group + broadcast messaging, announcements w/ acknowledgement tracking, notification pipeline, bell + live Socket.io. Build clean, flows verified by curl. | **Dev A: `lib/notifications.ts` is ready for you** — `notify({userId, type: NOTIFICATION.LEAD_BATCH_ASSIGNED, title, payload})`. Safe from cron. See the Day 3 entry in `DEV-B.md` for the snippet. |
| 4 | Lead Assignment: approval screen, 5-min auto-assign cron, transactional locking, logout-return, manual assign/reassign | Not started | Monitoring Engine: screen time, idle detection, break/pause control, activity event capture | Not started | |
| 5 | Agent Calling Workspace: Call List, Call button/clipboard fallback, disposition modal, callback scheduling | Not started | HR Module: employee profiles, documents+metadata, leave/holiday workflow, HR comms | Not started | |
| 6 | Agent Dashboard (no mgmt-only metrics), Do-Not-Call protection, Lead Timeline/Assignment History | Not started | Management Dashboard: KPIs, agent drill-down, approval queue, call recording access | Not started | |
| 7 | Reporting Engine — backend aggregation queries | Not started | Reporting Engine — frontend UI, exports, Management Score | Not started | |
| 8 | Audit & History: log capture, before/after diffing | Not started | Global Search/Filters, notification wiring, audit-log search UI | Not started | |
| 9 | VC Dialer real integration (or harden fallback), race-condition testing | Not started | NFR pass: HTTPS/sessions/hashing/validation/backups | Not started | |
| 10 | Shared: regression vs. 27 Acceptance Criteria, permission boundary testing | Not started | Shared: deployment, seed accounts, smoke test, handover | Not started | |

---

## Cross-Cutting Decisions Log

*(Append here whenever a decision affects both devs — schema changes, shared API contracts, naming conventions, library choices made mid-sprint. One line each, dated.)*

- 2026-09-08 — Stack finalized: Next.js + Postgres + Prisma + Socket.io, self-hosted VPS. (both devs agreed)
- 2026-09-08 — Split confirmed: Saad = Dev A (Core Calling Pipeline), teammate = Dev B (Ops & People Console).
- 2026-09-09 — **Lead locking model finalized (Dev A).** Current owner lives on `leads` (`status` / `assigned_to_id` / `locked_at` / `current_assignment_id`); `lead_assignments` is append-only history where `released_at IS NULL` marks the current holder. A hand-written **partial unique index** `lead_assignments_one_active_holder` makes a second open assignment impossible at the database level. It lives in the migration SQL because Prisma cannot express it — **do not drop it, and re-add it if a future migration ever recreates that table.**
- 2026-09-09 — **Auth = server-side `sessions` table + httpOnly cookie** (not stateless JWT). Logout and expiry are therefore real server-side events, which both LA-09 (return uncalled leads on logout) and the Monitoring Engine (screen time) depend on. Only the SHA-256 hash of the session token is stored.
- 2026-09-09 — **RBAC contract:** `requireRole()` / `requirePermission()` / `requireAuth()` in `src/lib/auth/rbac.ts`, applied at the API-route level. Permission keys and the default role matrix live in `src/lib/auth/permissions.ts` (33 keys). Prefer permission keys over role names so Admin can re-map at runtime. Page guards in `src/lib/auth/guard.ts` are UX only, never the boundary.
- 2026-09-09 — **Socket.io event contract lives in `src/lib/realtime/events.ts`** — both devs import from it, no string literals in routes. Path `/api/socket`, authenticated on the handshake, rooms `user:<id>` and `role:<name>`. Dev B: the `REQUEST_SUBMITTED` payload is already defined there for your Day 3 notification wiring — change it in that file, not locally.
- 2026-09-09 — **Prisma pinned to 6.19.3.** `npm install prisma` now resolves to 7.x ("Prisma Next"), a redesigned product with a different migration workflow (contracts, db signing). Both devs stay on 6.19.3 for this sprint.
- 2026-09-09 — **Next.js 16 note:** `middleware.ts` is renamed `proxy.ts`, and with a `src/` directory it must sit at `src/proxy.ts`. At the repo root it is silently ignored — no error, it just never runs.
- 2026-09-09 — **Dev B owns the final shape of these 9 framed tables** (drafted thin by Dev A on Day 1 only so the schema compiles): `messages`, `message_recipients`, `announcements`, `announcement_acknowledgements`, `hr_employees`, `hr_documents`, `leave_requests`, `reports_generated`, `management_scores`. Reshape freely. **Please coordinate before changing** `audit_log`, `notifications` or `activity_events` — Dev A writes to those from Day 3 / Day 8.
- 2026-09-09 — **Tables added beyond the original Day 1 list:** `sessions` (required by the auth model), `lead_requests` (LA-02/03 needs somewhere to record a request; your Day 6 approval queue reads it), `calls` (per-call capture — `dispositions` is the config lookup table, not the event), `role_permissions` (join table).
- 2026-09-09 — **Dev B context file is `DEV-B.md` at the repo root** (committed, so Dev A can read it), auto-loaded only in Dev B's sessions via a gitignored `CLAUDE.local.md` containing `@DEV-B.md`. Deliberately not `.claude/DEV-B.md` — see the next entry.
- 2026-09-09 — **⚠️ Dev A: `.claude/CLAUDE.md` is probably not auto-loading.** Claude Code auto-reads the **root** `CLAUDE.md` plus anything it `@`-imports. The root file here contains only `@AGENTS.md`, so `.claude/CLAUDE.md` is likely never read automatically despite its header saying it is — it has to be opened by hand each session. Fix if you want it automatic: add `@.claude/CLAUDE.md` to your own `CLAUDE.local.md`. Not changed unilaterally since it's Dev A's file.
- 2026-09-09 — **`docs/dev-b-phase-prompts.md` created (Dev B).** Day 2–10 prompt blocks mirroring `dev-a-phase-prompts.md`, which only covered Dev A's track.
- 2026-09-09 — **Dev B branching: one branch per day, `devb/day-N-<slug>`, merged to main at end of day.** Keeps main deployable and avoids a 10-day merge on Day 10. First branch: `devb/day-2-admin-config`.
- 2026-09-09 — **Dev B machine runs Postgres on host port 5433, not 5432.** A native PostgreSQL 18 Windows service owns 5432 there and shadows the container, so Prisma authenticates against the wrong server. Handled with a gitignored `docker-compose.override.yml` + local `.env` — **`docker-compose.yml` and `.env.example` are unchanged, so this affects nobody else.**
- 2026-09-09 — **Day 2 schema additions (Dev B), agreed in advance:** `groups` + `group_members` (AD-03, and CM-05 on Day 3), `system_settings` (key/value + Json — backs AD-06 dialer, AD-07 time/break, AD-08 notifications, AD-09 reports, so new settings never need a migration), `lead_field_definitions` (AD-05 builder; writes into the existing `leads.custom_fields`). **No Dev A table is touched.** `work_sessions` + `break_periods` follow on Day 4 for TM-01/TM-04.

---

- 2026-09-09 — **Day 2 migration applied (Dev B):** `20260909031108_day2_admin_config_groups_settings_fields`. 4 tables added (`groups`, `group_members`, `system_settings`, `lead_field_definitions`) + `LeadFieldType` enum. **29 tables total.** No Dev A table altered. Dev A: run `npx prisma migrate dev` after pulling.
- 2026-09-09 — **NF-07 index re-verified after the Day 2 migration.** `lead_assignments_one_active_holder` is still present. Since Prisma doesn't know about that hand-written index, **every migration from here on should re-check it** — `select indexname from pg_indexes where indexname='lead_assignments_one_active_holder';`
- 2026-09-09 — **⚠️ `prisma/seed.ts` now silently discards Admin permission changes.** It does `rolePermission.deleteMany()` then rebuilds from the `permissions.ts` baseline. Harmless on Day 1, but AD-02 (shipped today) makes `role_permissions` authoritative at runtime, so re-seeding reverts whatever the Admin configured. A warning comment sits at the call site. Proper fix (create-only, or an explicit `--reset-permissions` flag) is queued for Day 9's NFR pass. **Don't run `npm run db:seed` against a database whose permission matrix has been customised.**
- 2026-09-09 — **New permission keys (Dev B):** `admin.groups.manage`, `admin.settings.manage`. **35 total, was 33.** Both Admin-only. Re-seed to pick them up.
- 2026-09-09 — **`GET /api/admin/fields` is the AD-05 → LM-01 contract.** Returns active `lead_field_definitions`; each has a `key`, and the per-lead value belongs in `leads.custom_fields` under that key. `key` and `type` are immutable after creation (renaming would orphan stored values), and delete is a soft delete so historical leads still render. **Dev A: this is what your import column-mapper should offer beyond the fixed `leads` columns.**
- 2026-09-09 — **System settings are key/value + Json in `system_settings`**, with the registry of known keys in `src/lib/settings-catalogue.ts` (pure data, no Prisma — safe for the seed and the custom server) and accessors in `src/lib/settings.ts`. **Adding a setting means a registry entry, not a migration.** Secrets (`dialer.credentials`) are write-only: stored, never returned by the API.
- 2026-09-09 — **TM-03's 5 minutes is now configurable and must be read, not hard-coded.** `monitoring.config.inactivityMinutes` (default 5) via `getInactivityMs()`. Dev B's Day 4 idle sweep uses it; Dev A's Day 5 Call button should likewise read `dialer.config` rather than assuming.

- 2026-09-09 — **Messaging schema reshaped (Dev B, Day 3).** The Day 1 `messages.thread_key` + `message_recipients` frame is gone, replaced by `conversations` / `conversation_participants` / `messages`. Unread state is `conversation_participants.lastReadAt`, not a per-message flag. Also added `announcement_recipients` and the `ConversationType` / `AnnouncementAudience` enums. All inside Dev B's 9 framed tables — no Dev A table touched. 31 tables now; **NF-07 index `lead_assignments_one_active_holder` re-verified present after the migration.**
- 2026-09-09 — **NOTIFICATION PIPELINE IS LIVE — Dev A, this is your dependency.** `src/lib/notifications.ts` exports `notify()` / `notifyMany()` plus a `NOTIFICATION.*` catalogue covering all 9 SRS §15 types. It persists to `notifications` and emits `NOTIFICATION_NEW` in one call. **Use a `NOTIFICATION.*` constant, never a string literal** — Day 8's end-to-end wiring audit greps for them. It never throws into your flow: a failed notification will not roll back your lead assignment. No `server-only` in the import chain, so it is safe from the 5-minute auto-assign cron.
- 2026-09-09 — **`socket.io-client` moved devDependencies → dependencies (Dev B).** Client components import it; a production `npm ci --omit=dev` would have failed the build. `package-lock.json` changed — **run `npm ci` after pulling this.**
- 2026-09-09 — **`/messages` and `/announcements` are shared routes for all four roles**, added to every `NAV` list including `agent` (SRS §6.1 gives agents both screens). The notification bell is in `app-shell.tsx`, so it renders for every role. None of this touches TM-05: no monitoring metric is exposed anywhere in the comms UI.
- 2026-09-09 — **Broadcasts are N private DIRECT conversations, not one shared thread** (so a reply reaches only the sender), and **announcement audiences are frozen at publish time** in `announcement_recipients` (so the outstanding-acknowledgement list never changes retroactively as staff join or leave).

- 2026-09-09 — **New hand-relevant unique index: `conversations_pair_key_key` (Dev B, Day 3 fixes).** `conversations.pair_key` holds the two user ids of a DIRECT thread, sorted and joined. The unique index is what makes a duplicate DM impossible under concurrent create-or-reuse — same role `lead_assignments_one_active_holder` plays for lead ownership. Prisma DOES express this one (`@unique`), so it survives regeneration, but **the backfill in `day3_fix_direct_pair_key` does not** — if that migration is ever replayed against a database with existing conversations, keep the UPDATE statement. NULLs are unconstrained in a Postgres unique index, so an unbackfilled row is an unprotected row.
- 2026-09-09 — **Day 3 merged to main** (`e3f160b`). Sourcery review on PR #2 raised 4 issues; 3 were real and fixed before merge (duplicate-DM race, mark-read swallowing in-flight messages, missing `/notifications` route). The 4th — "the migration destroys existing message/announcement data" — was **not accepted**: `git log -S` confirms nothing wrote to those tables before Day 3, and `ADD COLUMN ... NOT NULL` fails loudly on a non-empty table rather than corrupting. Reasoning recorded in `DEV-B.md` so it isn't re-raised.

- 2026-09-09 — **Day 2 migration (Dev A):** `20260909125453_day2_lead_import_rows`. Adds `lead_import_rows` + `ImportRowStatus` / `DuplicateResolution` enums, and `lead_imports.detected_columns`. **32 tables.** No Dev B table touched. NF-07 index re-verified present after it, and after each of Dev B's three migrations.
- 2026-09-09 — **Dev B: the AD-05 contract Dev A actually consumes is the TABLE, not the route.** `GET /api/admin/fields` requires `admin.fields.manage`, which **Management does not hold** — and Management is who runs imports, so that route 403s for them. `src/lib/import/load-targets.ts` therefore reads `lead_field_definitions` through Prisma directly. Same contract (the field `key` → `leads.custom_fields`), no permission mismatch. Nothing for you to change; just don't assume the HTTP route is the integration point.
- 2026-09-09 — **Duplicate-detection rule (Dev A), for anyone building on `lead_import_rows`:** phone and email are PERSON-level and match alone; company name and website are ORGANISATION-level and only count when the CONTACT NAME also matches. Treating a shared company website as sufficient flagged different people at the same company as duplicates — which would suppress the multi-contact-per-company leads the client is paying to call. `duplicate_matched_on` records which fields matched so Management can judge, and LM-06's "keep both" is the escape hatch.
- 2026-09-09 — **Fixed in shared code (Dev A): `<Toaster />` was never mounted.** Every `toast()` call in the app was a silent no-op, including all six Day 2 Admin screens. Now mounted once in `src/app/(app)/layout.tsx`. **Dev B: your success/error toasts will start appearing after you pull** — no change needed on your side.
- 2026-09-09 — **⚠️ Shared cleanup for Day 9's NFR pass: `npx eslint src` fails with 13 errors.** 11 are in Dev B's client components (`react-hooks/set-state-in-effect` on the standard `useEffect(() => { void load() })` data-loader, plus a ref-during-render in `use-socket.ts`); 2 are Dev A's new import screens following the same house pattern. `next build` and `tsc --noEmit` are clean, so nothing is blocked. Worth one joint decision — fix the pattern everywhere or configure the rule — rather than each of us diverging.
- 2026-09-09 — **Dev A adopting the one-branch-per-day convention too.** First: `deva/day-2-lead-import`.
- 2026-09-09 — **Correction for Dev B: `.claude/CLAUDE.md` IS auto-loading for Dev A.** Claude Code reads it as a project instruction file alongside the root `CLAUDE.md`/`AGENTS.md`; it appears in Dev A's session context automatically, with no `CLAUDE.local.md` import needed. No change made.

## Scope Watch (from the build plan's honest scope warning)

If time gets tight near Day 9–10, these are the pre-agreed items to stub/defer first — don't cut anything else without flagging it here first:

1. Desktop workstation activity/screen-content monitoring — keep CRM screen-time + idle detection only.
2. 15-Day and Custom report types — ship after Daily/Weekly/Monthly/Punctuality.
3. Message file/document attachments — text messaging first.
4. Report scheduling — manual generation first.
